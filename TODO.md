# TODO

- [ ] Implement `GetByUsername` in user repository
- [ ] Implement `Update` (bio, avatar_url, username) in user repository
- [ ] Implement `Delete` in user repository
- [ ] Implement `GetByUsername` in user service
- [ ] Implement `UpdateProfile` in user service
- [ ] Implement `DeleteAccount` in user service
- [ ] Implement `GetUser` handler — parse `{username}` from path, return profile
- [ ] Implement `UpdateMe` handler — extract userID from context, parse JSON, update
- [ ] Implement `DeleteMe` handler — extract userID, verify password, delete
- [ ] Create post domain package (repository, service, handler, router)
- [ ] Implement post CRUD handlers
- [ ] Create comment domain package (repository, service, handler, router)
- [ ] Implement comment CRUD handlers
- [ ] Implement tag handler stubs (CreateTag, GetTag, ListTags, DeleteTag, AttachToPost, DetachFromPost)
- [ ] Implement category handler stubs (CreateCategory, GetCategory, ListCategories, UpdateCategory, DeleteCategory, AttachToPost, DetachFromPost)
- [ ] Register post domain in server/router.go
- [ ] Register comment domain in server/router.go
- [ ] Create `docker-compose.dev.yml` — postgres + redis
- [ ] Create `docker-compose.prod.yml` — postgres + redis + app
- [ ] Create `backend/Dockerfile` — multi-stage build
- [ ] Write auth service tests
- [ ] Write auth handler tests
- [ ] Write user repository tests
- [ ] Write user handler tests
- [ ] Write post repository + handler tests
- [ ] Write comment handler tests
- [ ] Write tag handler tests
- [ ] Write category repository + handler tests
- [ ] Write middleware tests (auth extraction, CORS, recovery)
- [ ] Run golangci-lint and fix warnings
- [ ] Run gofumpt on all files
- [ ] Remove `backend/build/` from git tracking
- [ ] Verify `.gitignore` excludes `backend/build/`
